/// <reference types="vite/client" />
interface Window { szeptucha: { getSettings():Promise<Settings>; saveSettings(s:Settings):Promise<Settings>; chooseFolder():Promise<string|null>; startRecording():Promise<void>; stopRecording(data:ArrayBuffer,mime:string):Promise<{text:string,path:string}>; correctSelection():Promise<{ok:boolean;message:string}>; onRecordingToggle(cb:(active:boolean)=>void):()=>void; onStatus(cb:(s:{type:string;message:string})=>void):()=>void; openFolder():Promise<void>; }; }
interface Settings { provider:'openai'|'gemini'; apiKey:string; model:string; folder:string; format:'txt'|'md'|'json'; recordHotkey:string; correctHotkey:string; launchAtStartup:boolean; language:string; }
