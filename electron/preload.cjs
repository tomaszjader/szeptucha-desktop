const{contextBridge,ipcRenderer}=require('electron');let recorder,chunks=[],stream;
async function start(){stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true}});chunks=[];recorder=new MediaRecorder(stream);recorder.ondataavailable=e=>e.data.size&&chunks.push(e.data);recorder.start();ipcRenderer.send('recording:state',true)}
function stop(){return new Promise((resolve,reject)=>{if(!recorder)return reject(new Error('Nagrywanie nie jest aktywne'));recorder.onstop=async()=>{try{const blob=new Blob(chunks,{type:recorder.mimeType});const data=await blob.arrayBuffer();stream?.getTracks().forEach(t=>t.stop());recorder=null;resolve(await ipcRenderer.invoke('audio:transcribe',data,blob.type))}catch(e){reject(e)}};recorder.stop();ipcRenderer.send('recording:state',false)})}
contextBridge.exposeInMainWorld('szeptucha',{getSettings:()=>ipcRenderer.invoke('settings:get'),saveSettings:s=>ipcRenderer.invoke('settings:save',s),chooseFolder:()=>ipcRenderer.invoke('folder:choose'),openFolder:()=>ipcRenderer.invoke('folder:open'),correctSelection:()=>ipcRenderer.invoke('text:correct'),startRecording:start,stopRecording:stop,onRecordingToggle:cb=>{const f=async(_,v)=>{try{if(v&&!recorder)await start();else if(!v&&recorder)await stop();cb(v)}catch(e){ipcRenderer.send('recording:error',e.message);cb(false)}};ipcRenderer.on('recording:toggle',f);return()=>ipcRenderer.removeListener('recording:toggle',f)},onStatus:cb=>{const f=(_,v)=>cb(v);ipcRenderer.on('status',f);return()=>ipcRenderer.removeListener('status',f)}});
